import { useEffect,useState,type FormEvent } from 'react';import { useNavigate } from 'react-router-dom';import { ApiError } from '../../api/apiClient';import { changeMyPassword,getMe,updateMe } from '../../api/meApi';import { GENERAL_AFFILIATIONS,STUDENT_DEPARTMENT_GROUPS } from '../../constants/departments';import { useAuth } from '../../auth';import './account-settings.css';
function AccountSettingsPage(){const navigate=useNavigate();const{logout}=useAuth();const[form,setForm]=useState({name:'',email:'',phone:'',affiliationType:'UNDERGRADUATE',department:''});const[pw,setPw]=useState({current:'',next:'',confirm:''});const[message,setMessage]=useState('');useEffect(()=>{getMe().then(user=>{if(user)setForm({name:user.name,email:user.email??'',phone:user.phone??'',affiliationType:user.affiliationType??'UNDERGRADUATE',department:user.department??''})}).catch(()=>setMessage('내 정보를 불러오지 못했습니다.'))},[]);const save=async(e:FormEvent)=>{e.preventDefault();try{await updateMe(form);setMessage('내 정보가 저장되었습니다.')}catch(error){setMessage(error instanceof ApiError?error.message:'저장하지 못했습니다.')}};const change=async(e:FormEvent)=>{e.preventDefault();if(pw.next!==pw.confirm){setMessage('새 비밀번호가 일치하지 않습니다.');return}try{await changeMyPassword(pw.current,pw.next);logout();navigate('/login')}catch(error){setMessage(error instanceof ApiError?error.message:'비밀번호를 변경하지 못했습니다.')}};return <main className="account-page"><header><p>MY ACCOUNT</p><h1>계정 설정</h1><span>연락처와 소속 정보를 최신 상태로 관리하세요.</span></header>{message&&<div className="account-message">{message}</div>}<div className="account-grid"><form onSubmit={save}><h2>내 정보</h2><label>이름<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>이메일<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>전화번호<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>소속 유형<select value={form.affiliationType} onChange={e=>setForm({...form,affiliationType:e.target.value,department:''})}><option value="UNDERGRADUATE">학부생</option><option value="GRADUATE">대학원생</option><option value="PROFESSOR">교수</option><option value="STAFF">직원</option><option value="OTHER">기타</option></select></label><label>소속<select value={form.department} onChange={e=>setForm({...form,department:e.target.value})}><option value="">선택</option>{STUDENT_DEPARTMENT_GROUPS.map(g=><optgroup key={g.label} label={g.label}>{g.departments.map(d=><option key={d}>{d}</option>)}</optgroup>)}<optgroup label="기타 소속">{GENERAL_AFFILIATIONS.map(d=><option key={d}>{d}</option>)}</optgroup></select></label><button>정보 저장</button></form><form onSubmit={change}><h2>비밀번호 변경</h2><p>변경 후 모든 기기에서 다시 로그인해야 합니다.</p><label>현재 비밀번호<input type="password" value={pw.current} onChange={e=>setPw({...pw,current:e.target.value})}/></label><label>새 비밀번호<input type="password" value={pw.next} onChange={e=>setPw({...pw,next:e.target.value})}/></label><label>새 비밀번호 확인<input type="password" value={pw.confirm} onChange={e=>setPw({...pw,confirm:e.target.value})}/></label><button>비밀번호 변경</button></form></div></main>}export default AccountSettingsPage;
